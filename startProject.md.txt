Npm init -y

Npm install sass --save-dev

Npm install parcel-bundler

Create index.html

Link scss file instead of .css

Change script in package.json 
To

  "scripts": {
    "dev": "parcel ./index.html",
    "build": "parcel build ./index.html"
  },

Add .gitignore file

Insert node_modules, .env to it
